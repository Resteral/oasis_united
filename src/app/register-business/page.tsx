import RegisterBusinessClient from './RegisterClient';

export const metadata = {
    title: 'Provision Your Boutique | Oasis United',
    description: 'Join the premier regional discovery network. Register your business on the Oasis United marketplace and reach 28+ verified citizens.',
};

export default function RegisterBusinessPage() {
    return <RegisterBusinessClient />;
}
