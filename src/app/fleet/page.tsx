import FleetClient from './FleetClient';

export const metadata = {
    title: 'Fleet Opportunities | Oasis United',
    description: 'Join the Oasis United fleet. Register for premium marketing slots or hourly delivery pay.',
};

export default function FleetPage() {
    return <FleetClient />;
}
